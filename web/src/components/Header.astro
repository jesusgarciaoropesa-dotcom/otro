---
import { categorias } from '../data/categorias';
import { withBase } from '../lib/url';
---

<header class="sticky top-0 z-40 border-b border-(--color-crema-oscuro) bg-(--color-crema)/95 shadow-sm backdrop-blur">
  <div class="mx-auto flex max-w-5xl items-center justify-between gap-4 px-4 py-2">
    <a href={withBase('/')} class="flex min-w-0 items-center gap-2 sm:gap-3">
      <img src={withBase('/images/logo-full.png')} alt="MiHuertoUrbano" width="64" height="64" class="h-12 w-12 shrink-0 sm:h-16 sm:w-16" />
      <span class="truncate font-(--font-display) text-base font-semibold text-(--color-hoja-oscuro) sm:text-xl">
        MiHuertoUrbano
      </span>
    </a>

    <div class="flex shrink-0 items-center gap-2">
    <a
      href={withBase('/buscar/')}
      aria-label="Buscar en la web"
      class="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-white text-(--color-hoja-oscuro) shadow-sm ring-1 ring-stone-200 transition hover:-translate-y-0.5 hover:shadow-md hover:ring-(--color-tierra)"
    >
      <svg class="h-4 w-4" viewBox="0 0 20 20" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <circle cx="9" cy="9" r="6.5" />
        <path d="M18 18l-4.35-4.35" stroke-linecap="round" />
      </svg>
    </a>

    <details class="categorias-menu group relative">
      <summary class="flex cursor-pointer list-none items-center gap-1.5 rounded-full bg-white px-4 py-2.5 text-sm font-semibold text-(--color-hoja-oscuro) shadow-sm ring-1 ring-stone-200 transition hover:-translate-y-0.5 hover:shadow-md hover:ring-(--color-tierra)">
        Categorías
        <svg
          class="h-3 w-3 transition-transform duration-200 group-open:rotate-180"
          viewBox="0 0 20 20"
          fill="currentColor"
          aria-hidden="true"
        >
          <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
        </svg>
      </summary>
      <div class="absolute right-0 z-50 mt-3 w-[min(92vw,36rem)] rounded-2xl bg-white p-3 shadow-xl ring-1 ring-black/5">
        <div class="grid grid-cols-2 gap-1 sm:grid-cols-3">
          {categorias.map((cat) => (
            <a
              href={withBase(`/categorias/${cat.slug}/`)}
              class="flex items-center gap-2 rounded-lg px-3 py-2 text-sm text-stone-700 transition hover:bg-(--color-crema) hover:text-(--color-tierra)"
            >
              <span class="text-lg" aria-hidden="true">{cat.emoji}</span>
              {cat.nombre}
            </a>
          ))}
        </div>
        <a
          href={withBase('/que-plantar-en-tu-balcon/')}
          class="mt-1 flex items-center justify-center gap-1.5 rounded-lg border-t border-stone-100 px-3 py-2.5 text-sm font-semibold text-(--color-hoja-oscuro) transition hover:bg-(--color-crema)"
        >
          🌱 Calculadora: ¿qué plantar en tu balcón?
        </a>
        <a
          href={withBase('/que-le-pasa-a-mi-planta/')}
          class="flex items-center justify-center gap-1.5 rounded-lg px-3 py-2.5 text-sm font-semibold text-(--color-hoja-oscuro) transition hover:bg-(--color-crema)"
        >
          🔍 ¿Qué le pasa a mi planta?
        </a>
        <a
          href={withBase('/articulos/')}
          class="flex items-center justify-center gap-1.5 rounded-lg px-3 py-2.5 text-sm font-semibold text-(--color-tierra) transition hover:bg-(--color-crema)"
        >
          Ver todos los artículos →
        </a>
      </div>
    </details>
    </div>
  </div>
</header>

<script>
  // Cierra el desplegable de categorías al pulsar fuera, al elegir un enlace,
  // o al pulsar Escape. <details>/<summary> no lo hace por sí solo.
  document.querySelectorAll<HTMLDetailsElement>('details.categorias-menu').forEach((menu) => {
    document.addEventListener('click', (event) => {
      if (!menu.contains(event.target as Node)) menu.open = false;
    });
    menu.addEventListener('click', (event) => {
      if ((event.target as HTMLElement).tagName === 'A') menu.open = false;
    });
    document.addEventListener('keydown', (event) => {
      if (event.key === 'Escape') menu.open = false;
    });
  });
</script>
